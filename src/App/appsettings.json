{
    "mockServerHost": "http://localhost:35200",
    "routes": [
        {
            "path": ".*",
            "method": "any",
            "validate": "all",
            "handler": "mock",
            "delay": 0
        }
    ]
}